apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: sota-ingress
  annotations:
    kubernetes.io/ingress.class: nginx
    cert-manager.io/cluster-issuer: letsencrypt-prod
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/proxy-body-size: "10m"
    nginx.ingress.kubernetes.io/rate-limit: "100"
    nginx.ingress.kubernetes.io/rate-limit-window: "1m"
spec:
  tls:
    - hosts:
        - sota-suite.dev
        - api.sota-suite.dev
      secretName: sota-tls
  rules:
    - host: sota-suite.dev
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: sota-web
                port:
                  number: 80
    - host: api.sota-suite.dev
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: sota-api
                port:
                  number: 80
---
apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: letsencrypt-prod
spec:
  acme:
    server: https://acme-v02.api.letsencrypt.org/directory
    email: admin@sota-suite.dev
    privateKeySecretRef:
      name: letsencrypt-prod
    solvers:
      - http01:
          ingress:
            class: nginx
