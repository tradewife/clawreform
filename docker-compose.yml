# NOTE: The GHCR image (ghcr.io/rightnow-ai/clawreform) is not yet public.
# For now, build from source using `docker compose up --build`.
# See: https://github.com/RightNow-AI/clawreform/issues/12

version: "3.8"
services:
  clawreform:
    build: .
    # image: ghcr.io/rightnow-ai/clawreform:latest  # Uncomment when GHCR is public
    ports:
      - "4200:4200"
    volumes:
      - clawreform-data:/data
    environment:
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY:-}
      - OPENAI_API_KEY=${OPENAI_API_KEY:-}
      - GROQ_API_KEY=${GROQ_API_KEY:-}
      - TELEGRAM_BOT_TOKEN=${TELEGRAM_BOT_TOKEN:-}
      - DISCORD_BOT_TOKEN=${DISCORD_BOT_TOKEN:-}
      - SLACK_BOT_TOKEN=${SLACK_BOT_TOKEN:-}
      - SLACK_APP_TOKEN=${SLACK_APP_TOKEN:-}
    restart: unless-stopped

volumes:
  clawreform-data:
